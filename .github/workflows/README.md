No GitHub Actions yet. Local publishing uses `pnpm comic:publish -- --slug <slug>`, which uploads and verifies new media, signs the R2 catalogue update, and verifies public reader surfaces without a Worker deployment. `scripts/deploy_latest.sh` is a compatibility wrapper for the same command. The one-time Worker release must first run `pnpm comic:seed-catalog`, then `pnpm deploy`.
